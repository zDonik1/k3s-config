# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  helmfile-nix:
    url: github:reMarkable/helmfile-nix
    inputs:
      nixpkgs:
        follows: "nixpkgs"

secretspec:
  enable: true
